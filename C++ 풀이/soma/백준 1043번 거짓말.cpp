#include <bits/stdc++.h>

using namespace std;

int n, m;
vector<int> truth;
vector<vector<int, int>> parties(m);
vector<vector<int, int>> adj(n);
vector<bool> visited(n, false);
vector<bool) truth_party(m, false);

int main() {
	
	
	
	
	return 0;
}

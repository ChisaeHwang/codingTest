#include <bits/stdc++.h>

using namespace std;

int dp[1001]; 
const int MAX = 987654321;

int main() {
	
   	vector<pair<int, int>> players; 
    int white, black; 

    // 입력이 끝날 때까지 계속 받음
    while (cin >> white >> black) {
        players.push_back({white, black}); 
    }
    
    
	
	return 0;
}
